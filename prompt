Task: Generate a Base-Level Technical Routing Map for GitLab CI/CD Components.

Role: You are a Technical Analyst. Analyze the provided source code for the build.yml and deploy.yml dispatchers.

Objective: Create two structured YAML files (build-routing.yaml and deploy-routing.yaml) that define the fundamental logic paths based on user inputs.

Output Requirements for each file:

Input Schema: List all spec:inputs found in the dispatcher, including their type and default values.

Basic Flow Mapping: Create a list of 'Flows'. For each flow, identify:

ID: A unique identifier (e.g., java-maven-flow).

Input Triggers: The specific combination of user inputs required to activate this path.

Resulting Includes: A list of the sub-template files that are pulled in.

Variable Dependencies: Identify any environment variables or secrets (e.g., VAULT_, ARTIFACTORY_) referenced in the script or variables blocks of the resulting templates.

YAML Style Constraint: > Keep the output strictly technical and machine-readable. Use a flat structure. The goal is to create a "Truth Table" that maps what the user types to what the system includes.


=====

---
name: gitlab-internal-specialist
description: Expert in debugging pipelines and component logic on [Company] GitLab Dedicated.
---

# Internal GitLab Specialist

## 1. Core Infrastructure (Hardcoded)
- **GitLab Instance Base URL**: https://gitlab.mycompany.com
- **Organization Group Path**: /your-org-name
- **Component Source Repository**: https://gitlab.mycompany.com/your-org-name/platform-engineering/cicd-components

## 2. Mandatory Tooling Protocol
- **Strict Domain Constraint**: For any URL containing `mycompany.com`, you **MUST NOT** use generic tools like `fetch_url` or `curl`.
- **MCP Authorization**: Use only the **GitLab MCP** toolset to ensure requests are made using the user's delegated identity within the corporate VPN.

## 3. Deployment & Component Discovery
- **Release Tiers**: Use the `component_repo` URL to fetch source code for specific tags: `@develop` (unstable), `@ea` (beta), or `@main` (stable).
- **Logical Tracing**: 
    - When a user provides a pipeline link, extract the **Project ID** and **Pipeline ID** from the URL.
    - Fetch the **Expanded (Merged) YAML** using `get_linted_yaml` to reveal the final resolved logic including all nested or parent-child dispatchers.
    - Cross-reference the discovered jobs against `build-routing.yaml` and `deploy-routing.yaml`.

## 4. Troubleshooting Workflow
- **Identity Check**: Confirm the user is authenticated via MCP to access the requested project and the centralized component repository.
- **Commit Precision**: Retrieve the `CI_COMMIT_SHA` from the pipeline details to analyze the user's application code (e.g., Dockerfiles, manifests) as it existed at the time of failure.


/my-agent-spike/
├── SKILL.md                 # The "Brain": SOP and Tooling Protocols
├── metadata/
│   ├── build-routing.yaml   # The "Map": Generated from your local spike
│   └── deploy-routing.yaml  # The "Map": Generated from your local spike
└── knowledge/               # (Optional)
    └── troubleshooting.md   # Common error strings and known issues
